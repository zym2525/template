import { StyleSheet, StatusBar } from 'react-native';
import theme from '@/style/theme'
import StyleConfig from '@/style/config'
import { tabStyle, setSize } from '@/utils/common'

export const styles = StyleSheet.create({
    a: {
        textAlign: 'center'
    }

})